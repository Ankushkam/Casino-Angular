export interface CurrentLevel {
    id?: string,
    level?: any,
    labelImage?: string,
    progress?: number,
    type?:string,
    points?: number,
    minPoints?: number,
    maxPoints?: number
}